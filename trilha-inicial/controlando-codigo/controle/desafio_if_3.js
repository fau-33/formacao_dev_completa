const x = 18;
const y = 10;

const operacao = "+";
let resultado = "";

if (operacao === "+") resultado = x + y;
if (operacao === "-") resultado = x - y;
if (operacao === "*") resultado = x * y;
if (operacao === "/") resultado = x / y;

console.log(resultado);
